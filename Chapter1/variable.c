#include <stdio.h>

int main() {
	// 整形
	int i = 10; // 表示i变量是整型变量，所谓整形变量就是指i只能是整形。
	printf("Variable i: %d\n", i);

	// 字符
	char char1 = 'W';
	printf("Variable char1: %c\n", char1);

	// 变量

	return 0;
}
